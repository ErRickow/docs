---
title: "PydanticAI"
description: "> Menggunakan PydanticAI dengan Neosantara."
---

PydanticAI adalah framework agen yang dibuat oleh tim Pydantic untuk menyederhanakan pembangunan aplikasi AI generatif tingkat produksi. Framework ini membawa filosofi desain ergonomis dari FastAPI ke pengembangan agen AI, menawarkan pendekatan yang familiar dan type-safe untuk bekerja dengan model bahasa.

## Menginstal Perpustakaan

```shell
pip install pydantic-ai -q
```

Atur kunci API Neosantara Kamu:

```shell
export NAI_API_KEY=***
```

## Contoh

```python icon="python"
from pydantic_ai import Agent
from pydantic_ai.models.openai import OpenAIChatModel
from pydantic_ai.providers.openai import OpenAIProvider
import os

# Hubungkan PydanticAI ke LLM di Neosantara
model = OpenAIChatModel(
    'nusantara-base',
    provider=OpenAIProvider(
        base_url="https://api.neosantara.xyz/v1",
        api_key=os.environ.get("NAI_API_KEY"),
    ),
)

# Setup agen
agent = Agent(
    model,
    system_prompt='Jadilah ringkas, balas dengan satu kalimat.',
)

result = agent.run_sync('Dari mana asal "hello world"?')  
print(result.output)
```

### Output

```python icon="python"
Hal itu bermula dari contoh-contoh Brian Kernighan untuk bahasa pemrograman B, yang kemudian dipopulerkan dalam buku "The C Programming Language".
```