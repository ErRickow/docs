---
title: 'Introduction'
description: '> Start using Neosantara Culture AI Intelligence'
---

## Base URL

The base URL for all requests to the Neosantara API is:

```plaintext with prefix /v1
https://api.neosantara.xyz/v1
```

## Authentication

All Neosantara endpoints are authenticated using API keys.
[Get your free API key](https://neosantara.xyz).

```bash icon="terminal"
curl -X POST https://api.neosantara.xyz/v1/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_NEOSANTARA_API_KEY" \
  -d '{"query": "Is 2045 the future of Indonesia?"}'
```

## Endpoints

<CardGroup cols={6}>
  <Card icon="magnifying-glass" horizontal href="/api-reference/chat/create-chat-completions">
    **`/chat/completions`**

    Completions Neosantara API Endpoint.
  </Card>
  
  <Card icon="file-lines" horizontal href="/api-reference/reasoning/enhanced-reasoning-completion">
    **`/reasoning`**

    Neosantara Reasoning API Endpoint.
  </Card>

  <Card icon="circle-nodes" horizontal href="/api-reference/moderations">
    **`/moderations`**

    Neosantara Content Moderation API Endpoint.
  </Card>
  
  <Card icon="circle-nodes" horizontal href="/api-reference/embeddings">
    **`/embeddings`**

    Neosantara Embeddings API Endpoint.
  </Card>
  
  <Card icon="circle-nodes" horizontal href="/api-reference/auth/key">
    **`/auth/key`**

    Neosantara API Endpoint KEY Metadata.
  </Card>
</CardGroup>