---
title: "Neosantara Documentation"
sidebarTitle: "Home"
description: "Documentation for Neosantara models, APIs, keys, usage, and framework integrations."
mode: "custom"
---

<div className="neo-home home-page">
  <div className="neo-home-shell">
    <section className="neo-doc-hero">
      <div className="neo-doc-hero-copy">
        <div className="neo-eyebrow">
          <Icon icon="book-open" size={14} />
          Developer documentation
        </div>

        <h1 className="neo-doc-title">
          Build with Neosantara.
        </h1>

        <p className="neo-doc-lead">
          Find the model you need, call it through OpenAI-compatible endpoints, and use framework integrations when your app needs agents, tools, or provider-agnostic switching.
        </p>

        <div className="neo-actions">
          <a href="/en/quickstart" className="neo-button neo-button-primary">
            Start quickstart
            <Icon icon="arrow-right" size={16} />
          </a>
          <a href="/en/models-overview" className="neo-button neo-button-secondary">
            Browse models
            <Icon icon="layers-3" size={16} />
          </a>
        </div>
      </div>

      <div className="neo-doc-map" aria-label="Documentation map">
        <div className="neo-map-header">
          <span>Start here</span>
          <Icon icon="route" size={16} />
        </div>

        <a href="/en/quickstart" className="neo-map-row">
          <span>01</span>
          <div>
            <strong>Quickstart</strong>
            <p>Create a key and make your first request.</p>
          </div>
        </a>

        <a href="/en/models-overview" className="neo-map-row">
          <span>02</span>
          <div>
            <strong>Models</strong>
            <p>Compare live models, capabilities, and pricing.</p>
          </div>
        </a>

        <a href="/api-reference/models/list-available-models" className="neo-map-row">
          <span>03</span>
          <div>
            <strong>API reference</strong>
            <p>Use generated endpoint docs from the backend spec.</p>
          </div>
        </a>
      </div>
    </section>

    <section className="neo-section">
      <div className="neo-section-head">
        <p>Choose a path</p>
        <h2>Use Neosantara from the surface that fits your app.</h2>
      </div>

      <div className="neo-path-grid">
        <a href="/en/quickstart" className="neo-path-card">
          <Icon icon="terminal" size={18} />
          <h3>OpenAI-compatible API</h3>
          <p>Use `/v1/chat/completions`, `/v1/responses`, embeddings, images, audio, files, and more with familiar client patterns.</p>
        </a>

        <a href="/en/integrations" className="neo-path-card">
          <Icon icon="blocks" size={18} />
          <h3>Framework integrations</h3>
          <p>Connect through native and provider adapters when you want agents, tools, orchestration, or one interface across providers.</p>
        </a>

        <a href="/en/about/rate-limits" className="neo-path-card">
          <Icon icon="gauge" size={18} />
          <h3>Usage and limits</h3>
          <p>Understand request limits, model costs, token accounting, and how usage behaves across tiers.</p>
        </a>

        <a href="/en/api-introduction" className="neo-path-card">
          <Icon icon="braces" size={18} />
          <h3>Endpoint reference</h3>
          <p>Jump into generated request schemas, parameters, response bodies, and playground examples.</p>
        </a>
      </div>
    </section>

    <section className="neo-section neo-spotlight-section">
      <div className="neo-section-head">
        <p>Integration spotlight</p>
        <h2>Native paths are available when you need them.</h2>
      </div>

      <div className="neo-spotlight-grid">
        <a href="/en/agno" className="neo-spotlight-card">
          <div>
            <span>Agents</span>
            <h3>Agno native</h3>
          </div>
          <p>Use `agno.models.neosantara.Neosantara` for multimodal agents, streaming, tools, and model switching.</p>
          <strong>Open Agno guide -></strong>
        </a>

        <a href="/en/any-llm" className="neo-spotlight-card">
          <div>
            <span>Provider layer</span>
            <h3>Any-LLM native</h3>
          </div>
          <p>Use provider id `neosantara` with `NEOSANTARA_API_KEY` for chat completions, Responses, images, and batch workflows.</p>
          <strong>Open Any-LLM guide -></strong>
        </a>
      </div>
    </section>

    <section className="neo-section neo-example-section">
      <div className="neo-section-head">
        <p>First request</p>
        <h2>Start from the API, then move into the framework you prefer.</h2>
      </div>

      <div className="neo-code-frame neo-readable-code">
        <div className="neo-code-card">
          <div className="neo-code-title-row">
            <div>
              <p>OpenAI-compatible request</p>
              <h2>Call a model with your API key.</h2>
            </div>
            <Icon icon="send" />
          </div>

          <CodeGroup>
            ```bash cURL
            curl https://api.neosantara.xyz/v1/responses \
              -H "Authorization: Bearer $NEOSANTARA_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{
                "model": "gemini-3.5-flash",
                "input": "Halo Neosantara"
              }'
            ```

            ```python Python
            import os
            from openai import OpenAI

            client = OpenAI(
                api_key=os.environ["NEOSANTARA_API_KEY"],
                base_url="https://api.neosantara.xyz/v1",
            )

            response = client.responses.create(
                model="gemini-3.5-flash",
                input="Halo Neosantara",
            )

            print(response.output_text)
            ```
          </CodeGroup>
        </div>
      </div>
    </section>
  </div>
</div>
