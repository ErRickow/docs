---
title: "Qwen Plus"
description: "Stable production alias with 1M context, unaffected by version churn. Verified: text generation."
---

import { ModelInfo } from '/snippets/model-info.jsx';
import { qwenPlusData } from '/snippets/model-data.jsx';

<ModelInfo {...qwenPlusData} />

## Quick Integration

```bash cURL
curl https://api.neosantara.xyz/v1/responses \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_NEOSANTARA_API_KEY" \
  -d '{"model":"qwen-plus","input":"Hello!"}'
```
