---
title: "Deepseek OCR"
description: "DeepSeek OCR model for extracting text from images with high accuracy. Supports URLs and base64 images."
---

import { ModelInfo } from '/snippets/model-info.jsx';
import { deepseekOcrData } from '/snippets/model-data.jsx';

<ModelInfo {...deepseekOcrData} />

## Quick Integration

```bash cURL
curl https://api.neosantara.xyz/v1/ocr \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_NEOSANTARA_API_KEY" \
  -d '{"model":"deepseek-ocr","image":"https://example.com/document.png"}'
```
