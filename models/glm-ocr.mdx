---
title: "GLM OCR"
description: "GLM-OCR model by Z.ai, specialized in high-accuracy text extraction, table recognition (HTML), and formula recognition (LaTeX). Note: Does not support visual grounding output."
---

import { ModelInfo } from '/snippets/model-info.jsx';
import { glmOcrData } from '/snippets/model-data.jsx';

<ModelInfo {...glmOcrData} />

## Quick Integration

```bash cURL
curl https://api.neosantara.xyz/v1/ocr \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_NEOSANTARA_API_KEY" \
  -d '{"model":"glm-ocr","image":"https://example.com/document.png"}'
```
