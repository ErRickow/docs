---
title: "GLM OCR"
description: "GLM-OCR model by Z.ai, specialized in high-accuracy text extraction, table recognition (HTML), and formula recognition (LaTeX)."
---

import { ModelInfo } from '/snippets/model-info.jsx';
import { glmOcrData } from '/snippets/model-data.jsx';

<ModelInfo {...glmOcrData} />

## Quick Integration

<CodeGroup>
```bash cURL
curl https://api.neosantara.xyz/v1/ocr \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_NEOSANTARA_API_KEY" \
  -d '{
    "model": "glm-ocr",
    "image": "https://example.com/image.jpg",
    "prompt": "Text Recognition:"
  }'
```

```python Python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.neosantara.xyz/v1",
    api_key="YOUR_NEOSANTARA_API_KEY"
)

# Using standard chat completions for vision
response = client.chat.completions.create(
    model="glm-ocr",
    messages=[
        {"role": "user", "content": "Text Recognition:"}
    ]
)

print(response.choices[0].message.content)
```

</CodeGroup>
