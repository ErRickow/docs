---
title: "GPT 5 4 Nano"
description: "GPT 5.4 Nano is the smallest and most affordable model in the GPT-5.4 family for high-volume sub-agent workflows."
---

import { ModelInfo } from '/snippets/model-info.jsx';
import { gpt54NanoData } from '/snippets/model-data.jsx';

<ModelInfo {...gpt54NanoData} />

## Quick Integration

```bash cURL
curl https://api.neosantara.xyz/v1/responses \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_NEOSANTARA_API_KEY" \
  -d '{"model":"gpt-5.4-nano","input":"Hello!"}'
```
